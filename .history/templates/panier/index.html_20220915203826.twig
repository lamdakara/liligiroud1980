{% extends 'base.html.twig' %}

{% block title %}Panier !{% endblock %}

{% block body %}
    <div class="container">
        <h3 class="color-vert">Votre panier</h3>
        <div>
            <table>
                <tbody>
                    {% for booking in bookings %}
                        {% set message = booking.message %}
                        <tr>
                            <td>
                               {{ message.titre }} de {{ message.duree }}min -  {{ message.prix }}€
                               <br />
                               {{ message.description }} 
                            </td>
                            <td>
                                <a href="#" class="color-vert">Dupliquer</a>
                            </td>
                            <td>
                                <a href="{{ path" class="color-vert">Modifier</a>
                            </td>
                            <td>
                                <a href="#" class="color-vert">Supprimer</a>
                            </td>
                        </tr>
                    {% endfor %}
                    
                    <tr></tr>

                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
